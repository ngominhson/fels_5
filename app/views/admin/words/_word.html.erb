<li>
 <%= link_to word.name, admin_word_path(word.id) %>
  :<%= word.description%>
  |<%= link_to "edit", edit_admin_word_path(word.id) %>
  |<%= link_to "delete", [:admin, word], method: :delete, data: { confirm: "You sure?" } %>
</li>