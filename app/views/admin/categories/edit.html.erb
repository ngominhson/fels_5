<% provide(:title, 'new Category') %>
<h1>Edit Categoty </h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@category) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :content %>
      <%= f.text_field :content %>

      <%= f.submit "Change Category", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>